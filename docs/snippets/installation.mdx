<AccordionGroup>
  <Accordion icon="square-terminal" title="Create a python environment">
    <Note>
      This is optional, but recommended.
    </Note>
    ```shell
    python -m venv synvya-env
    source synvya-env/bin/activate
    ```
  </Accordion>
  <Accordion icon="square-terminal" title="Install the Synvya SDK python package">
    ```shell
    pip install synvya-sdk
    ```
  </Accordion>
  <Accordion icon="square-terminal" title="Create a new directory for your project">
    ```shell
    cd ~
    mkdir synvya
    ```
  </Accordion>
</AccordionGroup>
